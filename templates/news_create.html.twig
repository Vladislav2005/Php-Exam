{% extends "base.html.twig" %}

{% block body %}

    <p class="fs-4 text-center">Creating new news</p>
    


    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.text) }}
    {# <div class="mb-3">
        <button class="btn btn-success" type="submit">Edit</button>
    </div> #}
    {{ form_end(form) }}


{% endblock %}
